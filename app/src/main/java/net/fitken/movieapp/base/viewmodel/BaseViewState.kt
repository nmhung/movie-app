package net.fitken.movieapp.base.viewmodel

/**
 * Define interface for state to use in ViewModel class
 * A ViewModel class might have many state, they should implements from this interface
 */
interface BaseViewState {
}